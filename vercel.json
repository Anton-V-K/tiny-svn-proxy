{
  "version": 2,
  "functions": {
    "api/**/*.js": {
      "maxDuration": 60
    }
  },
  "routes": [
    { "handle": "filesystem" },
    {
      "src": "^/api/(.+)$",
      "methods": [
        "GET",
        "HEAD",
        "POST",
        "PUT",
        "PATCH",
        "DELETE",
        "OPTIONS",

        // WebDAV / SVN-over-HTTP verbs commonly used by Subversion clients
        "PROPFIND",
        "PROPPATCH",
        "MKACTIVITY",
        "CHECKOUT",
        "MERGE",
        "REPORT",
        "MKCOL",
        "COPY",
        "MOVE",
        "LOCK",
        "UNLOCK"
      ],
      "dest": "/api/proxy?path=$1"
    }
  ],
  "rewrites": [{ "source": "/", "destination": "/api" }]
}
